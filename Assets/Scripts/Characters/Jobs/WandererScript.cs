using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WandererScript : CharacterScript
{
    // Start is called before the first frame update
    void Start()
    {
        ChangeCharacter(Farmer);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
